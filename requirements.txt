# Approved dependencies (STRATEGY.md §10 / CLAUDE.md). Pinned for reproducibility.
# MACD is computed manually with pandas (no pandas-ta).
alpaca-py==0.33.0
pandas==2.2.3
python-dotenv==1.0.1
requests==2.32.3
